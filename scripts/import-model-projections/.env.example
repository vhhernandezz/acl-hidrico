# Proyecto Supabase de Pucusana. Project Settings > API.
SUPABASE_URL=
# ⚠️ service_role, NO el anon key — este script bypassa RLS a propósito.
# Project Settings > API > service_role (mantenla en secreto, nunca la
# pongas en el frontend ni la subas a git).
SUPABASE_SERVICE_ROLE_KEY=
